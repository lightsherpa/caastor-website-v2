# Caastor website — Supabase CMS connection (public, safe to expose).
# Copy to `.env` for local dev, and add the same two vars in Vercel → Settings → Environment Variables.
# Leave blank to run the site on its bundled content with the CMS dormant.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
